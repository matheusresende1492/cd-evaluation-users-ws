package com.cd.evaluation.users.model.enums.roles;

public enum RoleEnum {
    ROLE_ADMIN, ROLE_USER;

//    public List<RoleEnum> getRoles(){
//        return Arrays.stream(RoleEnum.values()).collect(Collectors.toList());
//    }
}